[mypy]

# faster-whisper ships no py.typed marker / stubs (unlike anthropic, ollama,
# edge-tts, piper, pymupdf, all used elsewhere in this codebase without
# needing this).
[mypy-faster_whisper.*]
ignore_missing_imports = True
